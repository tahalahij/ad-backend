export enum RolesType {
  'ADMIN' = 'ADMIN', // system admin who can add operator
  'OPERATOR' = 'OPERATOR', // admin for a specified ip
  'CONTROLLER' = 'CONTROLLER', // controller admin with limited permissions
}
